using System;
using NUnit.Framework;

namespace Utils
{
	[TestFixture()]
	public class TestComputeTree
	{
		[Test()]
		public void TestEllipse()
		{
			/*/
			Graph.ComputeDelegate computeEllipse = (Graph.ObjectNode obj) => {

			};

			var graph = new Graph () {
				Objects = Cheap<Graph.ObjectNode>.FromArray (
					new Graph.ObjectNode ("ellipse1", computeEllipse,
				                    new Graph.PropertyNode ("X", -100.0),
				                    new Graph.PropertyNode ("Y", -100.0),
				              		new Graph.PropertyNode ("Width", 200.0),
				                    new Graph.PropertyNode ("Height", 200.0)
					)
				)
			};
			//*/
		}
	}
}

